# LaTeX to Astro Converter (LaTeX2Astro)

این پروژه ابزاری هوشمند و قدرتمند برای تبدیل کتاب‌های غیررسمی (Latex) به فرمت مناسب برای Astro (Markdown) است. این ابزار با تمرکز بر زبان فارسی، مدیریت خودکار تصاویر و انتشار مستقیم در گیت طراحی شده است.

## 🚀 ویژگی‌های کلیدی

- **پارس هوشمند LaTeX**: پیمایش بازگشتی فایل‌ها (`\input`, `\include`, `\subfile`) و استخراج متادیتای فارسی.
- **تبدیل با کیفیت بالا**: استفاده از Pandoc برای المان‌های پیچیده (ریاضی، جداول) همراه با فال‌بک بومی پایتون.
- **مدیریت هوشمند دارایی‌ها**: جستجوی خودکار تصاویر در مسیرهای گرافیکی و سازماندهی آن‌ها در ساختار Astro.
- **رابط کاربری جادویی (Wizard)**: فرآیند ۳ مرحله‌ای ساده برای تحلیل، ویرایش متادیتا و بازبینی فصل‌ها.
- **یکپارچگی با Git**: ثبت و ارسال خودکار تغییرات به مخزن وب‌سایت.
- **سیستم Slug فارسی**: تولید آدرس‌های وب (URLs) استاندارد و زیبا از عناوین فارسی.

## 🛠 نصب و راه‌اندازی

### پیش‌نیازها
1. پایتون ۳.۱۰ یا بالاتر.
2. پاندوک (اختیاری اما توصیه شده برای کیفیت بالاتر): [Pandoc Installation](https://pandoc.org/installing.html)

### نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

## 🖥 نحوه استفاده

برای اجرای رابط کاربری گرافیکی:
```bash
python src/gui/app.py
```

### مراحل کار:
1. **تحلیل**: فایل اصلی کتاب (`main.tex`) را انتخاب کنید. سیستم به صورت خودکار ساختار و فایل‌های ضمیمه را شناسایی می‌کند.
2. **متادیتا**: عنوان، نویسنده، چکیده و برچسب‌های کتاب را بازبینی و در صورت نیاز اصلاح کنید.
3. **بازبینی فصل‌ها**: لیست فصل‌ها را مشاهده، ترتیب آن‌ها را جابجا یا برخی را برای تبدیل غیرفعال کنید.
4. **پایان**: با کلیک بر روی دکمه نهایی، فرآیند تبدیل و در صورت تنظیم، ارسال به گیت انجام می‌شود.

## 📂 ساختار پروژه

```text
LaTex Converter/
├── src/
│   ├── core/           # منطق اصلی تبدیل و پارس
│   ├── models/         # مدل‌های داده‌ای (Book, Chapter, Config)
│   ├── gui/            # رابط کاربری CustomTkinter
│   ├── git/            # مدیریت عملیات Git
│   └── utils/          # ابزارهای کمکی (Slugify, etc.)
├── tests/              # تست‌های واحد و یکپارچگی
├── requirements.txt    # لیست کتابخانه‌های مورد نیاز
└── README.md           # همین فایل
```

## 🧠 معماری فنی (برای توسعه‌دهندگان)

- **Orchestrator**: وظیفه هماهنگی بین پارسر، مدل‌ها و مبدل را بر عهده دارد.
- **Label Registry**: تمام لیبل‌ها در مرحله پارس جمع‌آوری می‌شوند تا رفرنس‌های متقاطع (`\ref`) در مارک‌داون به درستی لینک شوند.
- **Manifest**: هر تبدیل یک فایل `manifest.json` تولید می‌کند که نگاشت دقیق فایل‌های منبع به مقصد را نشان می‌دهد.

## 📝 لایسنس
این پروژه برای استفاده شخصی در مجموعه MahdiSalem.com توسعه یافته است.
